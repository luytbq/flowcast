# Case mà bản Go cố ý khác bản tham chiếu, kèm lý do. Test Go bỏ qua chúng khi
# so với đáp án của Python; hành vi mới được chốt bằng case riêng ở chỗ khác.
# Mỗi dòng: <tên case>: <lý do>, hoặc cli:<tên bản ghi>: <lý do> cho một bản ghi
# CLI.
991-invalid-no-lane: bảng không có lane là flowchart hợp lệ, chốt ở conformance/flowchart
cli:unknown-extension: thông báo đuôi lạ liệt kê thêm .mmd, định dạng mermaid
cli:dotfile-has-no-extension: thông báo đuôi lạ liệt kê thêm .mmd, định dạng mermaid
18-labels-crowded: condition chỉ nhận dây vào từ đỉnh, chốt ở conformance/diverge
42-place-merge-nested: condition chỉ nhận dây vào từ đỉnh, chốt ở conformance/diverge
cli:merge-fuzz-137: condition chỉ nhận dây vào từ đỉnh, chốt ở conformance/diverge
cli:merge-fuzz-1: merge dùng bề rộng lane chưa làm tròn khi lane chưa bị sửa, bề rộng pool không lệch 0.01px
cli:merge-fuzz-38: merge so tỉ lệ cổng sau khi làm tròn như lúc ghi, dây chưa sửa giữ vị trí nhãn
cli:merge-fuzz-53: merge so tỉ lệ cổng sau khi làm tròn như lúc ghi, dây chưa sửa giữ vị trí nhãn
